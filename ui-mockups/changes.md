1. make all the text a bit larger
2. the first menu item should be the name of the species not "overview"
3. move the collection details to the bottom (both the section and the menu item)
4. move the names (except the scientific synomyns into the "overview")
5. move the remaining nomenclature section (and menu item) to the bottom (above collection)
6. We get rid of the tiers in the menu. Remove description, keep its sub-sections as top-level menu items. remove ecology similarly. Get rid of conservation. make the sub-menu items to be top-level and move reforestation above status. but keep status above collection
7. human uses remains, but we don't show sub-menu items in the menu (keep the corresponding sections however)
8. remove basic info section completely (we already have the info being shown in overview)
